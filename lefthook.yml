pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.ts"
      run: npx eslint --fix {staged_files}
      stage_fixed: true
    format:
      glob: "*.ts"
      run: npx prettier --write {staged_files}
      stage_fixed: true
